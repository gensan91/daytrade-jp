# KNOWLEDGE — デイトレ自動化プロジェクト 技術知見

**追記のみ。めったに読まない。** 現在の状態は `STATE.md` を参照。

検証レベル：【公式】＝公式ドキュメントに記載／【実測】＝自分で確かめた／【推測】＝未検証

---

## 1. 楽天証券 RSS：単位

| 項目 | 単位 | 根拠 |
|---|---|---|
| 売買代金 | **千円** | 【実測】トヨタ 73,345,580 → 733億円。出来高×株価と一致 |
| 時価総額 | **百万円** | 【実測】極洋 55,620 → 556億円 |
| 単位株数 | 株 | 【実測】調査した444銘柄すべて100 |

公式ヘルプに単位の記載はない。

---

## 2. 楽天証券 RSS：銘柄数上限とRTD登録

- 【公式】同時取得の上限は**500銘柄**（証券口座単位・Excel全体で共有）
  https://marketspeed.jp/ms2_rss/onlinehelp/ohm_002/ohm_002_08.html
- 【実測】登録は **`xl/volatileDependencies.xml`（.xlsm内）に永続化される**。
  ファイルを保存して閉じても消えず、次に開いた瞬間に再登録が走る。
- 【実測】**解放するには次のどちらか。切断→再接続は不要だった。**
  1. RSS数式を**値に固定**する（コピー→値貼り付け、またはVBAで `.Value = .Value`）
  2. シートごと削除して保存
- 【実測】上限に達すると各セルが `表示銘柄数超過` になる。この状態も**ファイルに保存される**ため、
  開き直しても自動では回復しない。
- 【実測】一度に450銘柄を投げると、850トピック中800が超過になった。

### 作業を中断するときの正しい手順
数式を残したまま保存すると次回オープン時に再登録が走る。
かといって保存せずに閉じると**作業結果が失われる**（`選別候補` を一度この方法で失った）。
→ **RSS数式だけ値に固定してから保存する。**

---

## 3. 楽天証券 RSS：RssMarket（市況情報）

- 【公式】取得項目は**148種類**。一覧はこちら
  https://marketspeed.jp/ms2_rss/onlinehelp/ohm_002/ohm_002_07.html
- よく使う項目名（すべて実測で動作確認済み）
  - 銘柄名称 / 市場部略称 / 現在値 / 前日終値 / 始値 / 高値 / 安値
  - 出来高 / 売買代金 / 時価総額 / 単位株数 / 決算発表日
  - **信用貸借区分**（1＝貸借＝空売り可、2＝信用）／ 最良売気配値 / 最良買気配値
- 【実測】新形式の英数字混在コード（166A、167A等）もそのまま渡せる

### ⚠ 決算発表日フィールドは「予定日」ではない
- 【実測】**大半が「直前に発表した実績日」を返す**。
  - コア30候補126件のうち **102件（81%）が過去日**
  - 主要10銘柄のサンプルでも 9/10 が過去日
- したがって `= 今日 Or = 明日` という判定は**原理的にほぼ成立しない**。
  イベント銘柄が0件だったのはこれが原因（日付跨ぎのせいではない）。
- 代替：**J-Quants API の「決算発表予定日」**（Freeプランでも取得可）
  https://jpx.gitbook.io/j-quants-ja/outline/data-spec

---

## 4. 楽天証券 RSS：RssChartPast（日足履歴）

**公式ヘルプに書かれていない挙動が多い。これを知らずに実装すると必ずハマる。**

```
=RssChartPast(ヘッダー行, 銘柄コード, 足種, 起点日付, 本数)
```

1. 【実測】**スピル関数ではない。** 数式セルは `完了` 等のステータス文字列を返すだけ
2. 【実測】`ヘッダー行` は出力先ではなく**入力**。欲しい項目名を書いた行を渡す。
   **省略が正解**（省略するとRSSが全項目のヘッダーを数式セルの1行下に自動で書く）。
   空のセル範囲を渡すと「ヘッダー行に有効な項目名が存在しません」エラー
3. 【実測】データは**数式セルの2行下**から始まる（1行下はヘッダー）
4. 【実測】**起点日付から未来方向へ**指定本数ぶん取得する。並びは**古い順**
   → **当日を起点にすると1本しか返らない**（最初の実行が空だった原因）
5. 【実測】起点が非営業日なら翌営業日から始まる（エラーにはならない）
6. 【実測】列は 1銘柄名称 2市場名称 3足種 4日付 5時刻 6始値 7高値 8安値 9終値 10出来高
7. 【実測】**銘柄名称は最初のデータ行にのみ入る**。2行目以降は空欄
   → 行数を数えるときは**日付列**を見ること
8. 【実測】データの末尾に `--------` の行が入る
9. 【実測】銘柄コード書式（`7203` / `7203.T` / `72030.T`）は結果に影響しない
10. 【実測】速度は**1銘柄0.1秒**。10銘柄を横並びで同時投入すれば151銘柄で6秒

※ 売買代金は取得項目にないので **出来高×終値** で近似する

---

## 5. JPX 東証上場銘柄一覧

- 【公式】`data_j.xls` を https://www.jpx.co.jp/markets/statistics-equities/misc/01.html から入手
- 毎月第3営業日に前月末データへ更新
- プライム＋スタンダードの内国株式のみで **3,117銘柄**（2026/8時点）
- 保存先：`C:\Users\gensa\OneDrive\株・資産管理\銘柄表\data_j.xls`
  （`modScreening.JPX_FILE_PATH` にハードコード）

---

## 6. VBA / Excel の落とし穴

- **.bas は必ず Shift-JIS(CP932) で保存する。** UTF-8だと日本語が文字化けする
- `⓪` などCP932にない文字はコメントにも使えない
- VBAから `.Formula` で数式を書くと、Excelが互換のため `@`（暗黙的インターセクション）を
  自動挿入する。配列を返す関数では `.Formula2` を使う
  ※ ただし RssChartPast はスピル関数ではないので、今回この違いは無関係だった
- `Application.Wait` はメッセージ処理を止めるため、RTDの待機ループには使わない。`DoEvents` を使う
- `Application.Calculate` はブック全体の再計算。ループ内で呼ぶと重い

---

## 7. 銘柄選定に関する実証データ（2026/8/7時点）

### 決算による汚染
- Watchlist 444銘柄のうち **233件（52%）が直近7日以内に決算発表済み**
- 決算直後グループと それ以外の比較：

| グループ | 件数 | 日中値幅率(中央値) | 売買代金(中央値) |
|---|---|---|---|
| 決算後7日以内 | 233 | **3.62%** | 5.34億 |
| それ以外 | 211 | **2.38%** | 5.21億 |

→ **売買代金がほぼ同じでも値幅は1.5倍。** 売買代金だけでは両者を区別できない。
→ 8月上旬は第1四半期決算のピーク。この時期のスクリーニングは母集団が歪む。

### 単日スナップショットの誤差
- 20日平均で見ると **22/151件（15%）** が「売買代金3〜10億」レンジから外れていた
  （10億超が17件、3億未満が5件）
- 最大2.79倍のズレ（単日7.4億 → 20日平均20.5億）

### ギャップ発生頻度（151銘柄×20日＝3,020日の実測）

| 閾値 | 発生日数 | 発生率 | 20日で1度でも発生した銘柄 |
|---|---|---|---|
| 3%以上 | 110日 | 3.64% | 79/151 |
| 5%以上 | 41日 | 1.36% | 38/151（25%） |

→ 30銘柄を監視した場合、5%閾値では**1日0.55件**（約2日に1件）

### コア30の性格
- 20日平均値幅率の中央値 **3.68%**（母集団106件は2.36%）
- 20日平均売買代金の中央値 7.2億
- 単元価格の中央値 174,100円（上限30万円制約下）

---

## 8. スコアリング設計（現行版）

対象：単元価格30万円以下・信用貸借区分=1・決算±7日除外・20日平均売買代金3億以上（103件）

```
スコア = 0.45 × z(20日平均値幅率)
       + 0.25 × z(log(20日平均売買代金))
       + 0.20 × z(ギャップ3%以上の日数)
       - 0.10 × z(値幅率の変動係数)
```

- 閾値で絞るのではなく**順位づけて上位30件**を取る方式。
  相場環境が変わっても常にちょうど30件出るので、閾値の手動調整が不要
- 重みは暫定。**紙トレの実データが出るまで根拠がない**ので触らない

---

## 9. 設計上の教訓

- **同じ事実を2箇所に書くとズレる。** Watchlist と コア30候補一覧 を別々に重複判定していた結果、
  318件の食い違いが発生し、原因の特定が不能になった
- **手で書いた進捗表は必ず実態とズレる。** 「バッチ5で停止」とメモにあったが、
  実データを解析したら7バッチ全部完了していた。進捗はプログラムに報告させる
- **元データを残し、派生物は毎回作り直す。** `選別候補` を失ったが、
  `スナップショット` から1秒で再生成できたため実害がなかった
- **未知のAPIは1引数ずつ変えて確かめる。** 複数を同時に変えると、
  動いていた部分まで壊して原因が分からなくなる（実際に4回空振りした）
- **エラー時に状態を消さない。** 診断シートをクリアしてからエラー通知を出したため、
  証拠を自分で消してしまったことがある。`Err.Number` と `Err.Description` も必ず表示する
