# CLAUDE.md — daytrade-jp プロジェクト引き継ぎサマリー

> 配置場所：リポジトリルート（`daytrade-jp/CLAUDE.md`）。Claude Code が起動時に自動読込する。
> 作成日：2026/8/22（土）。**作成時に xlsm 実物・GitHub 実物の両方を監査済み**（監査結果は §2.0）。

---

## 0. 最初に読むこと：このリポジトリでの Claude の権限と限界

### 0.1 一次情報の優先順位（これを守らないと必ず事故る）

| 順位 | 情報源 | 位置づけ |
|---|---|---|
| 1 | **`デイトレ用.xlsm` の中身**（VBA・シート値） | **唯一の正**。実際に動いているのはこれだけ |
| 2 | `Trades` シート / `data/trades.csv` | トレード実績の正 |
| 3 | `STATE.md` | 現在地。**ただし過去に3回、実物とズレていた** |
| 4 | `KNOWLEDGE.md` | 技術知見。追記のみ。めったに読まない |
| 5 | Claude の記憶・チャット履歴 | **信用しない** |

> **プロジェクト憲章：「実装済み」と「実動作確認済み」は別物として扱う。**
> `[L]`（8/19）、`[M]`（8/20）は、STATE.md に「実装済み」と書いてあったのに動いていなかった。
> 何かを「済み」と判断する前に、**xlsm を実際に開いて（＝Python で読んで）確かめる。**

### 0.2 Claude Code にできること／できないこと

| | 内容 |
|---|---|
| ✅ できる | `src/*.bas` `*.md` `data/*.csv` の編集、git 操作、`デイトレ用.xlsm` を Python（openpyxl / oletools）で**読んで監査**する、集計・統計計算、監査スクリプトの作成 |
| ❌ できない | Excel の起動、VBA の実行、マーケットスピードII RSS への接続、**xlsm へのコード書き戻し**（VBAプロジェクトはバイナリ。VBE 経由の手作業でしか入らない） |

**帰結：Claude が `src/*.bas` を書き換えても、それだけでは本番に反映されない。**
必ず「ユーザーが VBE で 解放（削除）→ Import File」という手作業が挟まる（§3.4）。
コードを直したら**必ず反映手順をセットで提示する。**

---

## 1. プロジェクト概要 & アーキテクチャ

### 1.1 目的

日本株の**ギャップアップ・デイトレードの半自動化システム**。現在は**紙トレード検証フェーズ**。

- **最終目標**：統計的にクリーンなサンプルを 20 件集め、そこから最適 TP/SL を逆算してライブ運用へ
- **現在の到達点**：主サンプル（寄り付き型）**3 / 20 件**
- **鉄の規律**：**20 件が揃うまでパラメータ（B1〜B14）は一切変更しない。** 途中で変えると比較対象が消える

### 1.2 技術スタック

| 層 | 技術 | 備考 |
|---|---|---|
| 実行基盤 | Windows + Excel 365 | UI は Excel の UserForm |
| データ取得 | **マーケットスピードII RSS**（楽天証券） | `RssMarket()` = リアルタイム、`RssChartPast()` = 日足 |
| ロジック | **VBA**（標準モジュール9本＋ThisWorkbook＋UserForm1本） | 外部ライブラリなし |
| 永続化 | Excel シート（15枚） | DB は使わない |
| スケジューラ | `Application.OnTime` の自己再帰チェーン | **これが最大の脆弱点**（§2.4 issue#8） |
| バージョン管理 | GitHub（`gensan91/daytrade-jp`） | xlsm はバイナリごとコミット |
| 監査ツール | **Python 3 + openpyxl + oletools(olevba)** | Claude Code 側の主力。**書き込みには使わない** |

### 1.3 データフロー

```
[MS2 RSS] --RssMarket--> Watchlist C:F (30銘柄×4項目=120数式)
                              |
                    modMonitor.CheckGapSignals（3秒周期 / 8:59-10:00）
                    gapPct = (現在値 - 前日終値) / 前日終値 * 100
                              |  閾値3%超の最大1本だけ発火（1日1回）
                              v
                    [手動] 楽天証券で発注 → 操作パネル「エントリー記録」
                              |
                    Position 2行目（唯一の作業行）
                    modPosition.CheckPositionStatus（3秒周期）
                    → 利確+3% / 損切-2% / 15分ランナー失格 / 15:00強制
                              |
                    [手動] 決済 → 操作パネル「決済記録」
                              |
                    modPosition.ArchiveTrade → Trades シートに22列で追記
                              |
                    StartAfterExitTracking（15秒周期 → 15:31 で FinalizeTracking）
                              v
                    Trades O/P/Q列（大引けMFE/MAE/終値ベース損益率）

[引け後] modSnapshot.SnapshotWatchlist → スナップショット（始値含む14項目）
         → 翌営業日夜に SS_MMDD へリネーム退避
[月次/週末] modScreening + modDaily7 → 選別候補 → 日足集計（コア30の選定根拠）
```

### 1.4 ディレクトリ構造と設計方針

```
daytrade-jp/
├── CLAUDE.md              # 本ファイル。Claude Code 用の入口
├── README.md              # 人間用の入口。src/ の取り扱い注意が書いてある
├── STATE.md               # 現在地。★毎回まるごと上書き★
├── KNOWLEDGE.md           # 技術知見。★追記のみ・削除しない★
├── .gitignore             # data_j.xls（JPX銘柄マスタ）を除外
├── デイトレ用.xlsm         # ★唯一の完全バックアップ★（週1コミット）
├── src/                   # VBA のテキストバックアップ
│   ├── mod*.bas           # 標準モジュール9本（Shift-JIS/CRLF）
│   ├── ThisWorkbook.cls   # ★Import不可・貼り付け専用★（Attribute行を除去済み）
│   └── frmController.frm.txt  # イベントコードのみ。レイアウト(.frx)は含まない
└── data/
    └── trades.csv         # Trades シートの書き出し（22列）
```

**設計方針**

1. **`src/` はコードのバックアップであってシステムのバックアップではない。**
   `frmController` のボタン配置・サイズ・キャプションは `.frx` にあり、テキスト化できない。
   **UserForm を復元できるのは `デイトレ用.xlsm` だけ。**
2. **STATE.md は上書き運用**なので、20件集計時に生き残る保証がない。
   → **恒久的に残すべき注記は `Trades` の U列（備考）に書く。** これが本当の恒久履歴。
3. マジックナンバーはコードに書かない。**数値は `Config` シート、列番号は `modConfig` の定数**。

### 1.5 シート構成（15枚）

| シート | 役割 | 状態 |
|---|---|---|
| `Config` | パラメータ14項目（A列ラベル / B列値） | ✅ |
| `Watchlist` | コア30銘柄。C:F に RSS数式120個 | ✅ 稼働 |
| `Position` | **2行目のみが作業行**。A〜T列 | ✅ 稼働 |
| `Log` | シグナル・決済・復旧・日次サマリ | 18行 |
| `Trades` | **恒久履歴・22列**。唯一の正 | 7行（ヘッダ+6件） |
| `スナップショット` | 引け後の30銘柄×14項目（**始値を含む**） | 数式固定済み |
| `SS_0819` / `SS_0820` | 上記の日次退避 | 2日分 |
| `日足集計` | コア30選定の根拠（20日分の統計・151銘柄） | 152行 |
| `選別候補` | スクリーニング中間結果 | 152行 |
| `JPX銘柄マスタ` | 3,117銘柄 | 3,118行 |
| `_work` | `modDaily7` の作業領域 | — |
| `コア30候補一覧` | 選定結果 | 31行 |
| `全銘柄候補` / `イベント銘柄一覧` | **空が正常** | 空 |

### 1.6 モジュール責務

| モジュール | 行数 | 責務 |
|---|---|---|
| `modConfig` | 232 | Config読込＋**全定数の定義**（シート名・列番号）＋`ShowLoadedConfig` |
| `modMonitor` | 509 | ギャップ監視チェーン、シグナル発火、日次サマリ |
| `modPosition` | 891 | エントリー/決済記録、ポジション監視、Trades退避、大引け追跡 |
| `modWatchdog` | 80 | OnTime チェーンの生存監視と再武装（30秒閾値） |
| `modSnapshot` | 219 | 引け後スナップショット取得＋数式固定 |
| `modScreening` | 626 | JPX全銘柄からの月次スクリーニング（バッチ実行） |
| `modDaily7` | 437 | `RssChartPast` による日足統計。**真の寄り付きギャップはここで測れる** |
| `modNotify` | 60 | 通知音 |
| `modUIButtons` | 19 | `ShowController`（UserForm モードレス表示） |

---

## 2. 現在の実装状況 & 残タスク

### 2.0 監査結果（2026/8/22 実施・一次情報ベース）

**A. xlsm 実物**

| 検査項目 | 結果 |
|---|---|
| 標準モジュール | **9本ちょうど**。`modConfig1` / `ThisWorkbook1` なし ✅ |
| シートモジュール | `Sheet1`〜**`Sheet15`** ＋ ThisWorkbook（issue#14 の通り増殖中） |
| Config B13 | **15:31:00** ✅ / B1=3, B2=-2, B3=3, B14=0.4 ✅ |
| RSS数式の残存 | Watchlist 120 ＋ Position E2 1 = **121**。スナップショット系は**全て値固定済み** ✅ |
| `[N]`（決済理由の番号選択） | `AskExitReason` / `SuggestReasonIndex` / `ReasonByIndex` の存在を確認 ✅ |
| Trades | 6件。K列は全行が新語彙（利確/損切/時間切れ）で統一済み ✅ |
| `スナップショット` シート | 8/21取得分が未リネームで在席（正常。8/24夜に `SS_0821` へ） |

**B. GitHub リポジトリ — STATE.md の記述に事実誤認あり**

STATE.md 末尾に「**KNOWLEDGE.md / src/*.bas / data/trades.csv が未投入のまま。実質バックアップが存在しない状態**」とあるが、**これは現時点で誤り。** 実物を叩いて確認した結果：

| ファイル | 実際 | 判定 |
|---|---|---|
| `README.md` / `STATE.md`(8/21版) / `KNOWLEDGE.md` / `.gitignore` | 存在 | ✅ |
| `src/*.bas` 9本 / `src/ThisWorkbook.cls` / `src/frmController.frm.txt` | 存在 | ✅ |
| `デイトレ用.xlsm` | 存在。**MD5 が手元の 8/21 版と完全一致** | ✅ |
| **`src/modPosition.bas`** | **785行。実物は890行 → `[N]` が未反映** | ❌ **要push** |
| **`data/trades.csv`** | **5件。8/21 の 9278 行が欠落** | ❌ **要更新** |
| `src/ThisWorkbook.cls` | 末尾の空行1つだけ差分。実質同一 | ✅ |
| `ROUTINE.md` | 404。ただし README も約束していない | 実害なし |

> **→ 「バックアップが無い」のではなく「最後の1コミットだけ取りこぼした」が正しい状態。**
> STATE.md のその段落は次回更新時に削除すること。

**C. 手元の `/mnt/project/STATE.md` は 8/20 版で古い。GitHub の 8/21 版が最新。**

### 2.1 トレード実績（2026/8/21 時点）

| 日付 | 銘柄 | 検知 | エントリー | 決済 | 理由 | 損益率 | コスト後 | 区分 |
|---|---|---|---|---|---|---|---|---|
| 8/10 | 4813 ＡＣＣＥＳＳ | 9:00:16 | 393 | 404.8 | 利確 | +3.00% | +12,274 | 寄り付き型 |
| 8/13 | 7322 三十三FG | 9:00:44 | 1975 | 1992 | 時間切れ | +0.86% | +1,820 | 寄り付き型 |
| 8/14 | 8550 栃木銀行 | 9:08:17 | 1115 | 1132 | 時間切れ | +1.52% | +5,016 | ザラ場型 |
| 8/19 | 9278 ブックオフGHD | 9:00:13 | 2858 | 2790 | 損切 | -2.38% | -7,943 | 寄り付き型 |
| 8/20 | 7513 コジマ | 9:01:15 | 1454 | 1442 | 時間切れ | -0.83% | -5,345 | ザラ場型 |
| 8/21 | 9278 ブックオフGHD | 9:05:27 | 3075 | 3020 | 損切 | -1.79% | -6,730 | ザラ場型 |

**3勝3敗／コスト後累計 -908円**（検算済み）。寄り付き型 3件 / ザラ場型 3件。

### 2.2 実装済み・検証済みの機能

| 記号 | 内容 | 実装 | 実動作検証 |
|---|---|---|---|
| `[G]` | Watchdog 誤検知の解消 | — | ✅ クローズ |
| `[H]` | 日次サマリを Log に1行残す | ✅ | ✅ 8/20・8/21 |
| `[J]` | Q列を追跡完走時にのみ確定 | ✅ | ✅ 8/21 |
| `[K]` | スナップショットの完全再計算 | ✅ | ✅ 8/20 |
| `[L]` | 起動時に「追跡未完走」を警告 | ✅ | ✅ 8/19 |
| `[M]` | **Config 時刻読込のDouble対応**＋`FindConfigRow`（xlWhole優先）＋`ShowLoadedConfig` | ✅ | ✅ 8/20 |
| — | **B13 = 15:31 の実効果** | ✅ | ✅ **8/21**（T2=15:31:03／Q7が当日終値2,955と一致） |
| `[N]` | **決済理由を番号選択（5種固定）に変更** | ✅ 8/21 | ❌ **未検証。次のシグナル日が初回テスト** |

### 2.3 現在進行中の最大の論点：`ギャップ率` は寄り付きギャップではない

```vba
gapPct = (current - prevClose) / prevClose * 100   ' current = 「現在値」であって「始値」ではない
```

実際に測っているのは「**前日終値比の騰落率（時点値）**」。検証可能な3件が**3件とも**真のギャップではなかった：

| 日付 | 銘柄 | 真の寄り付きギャップ | システム判定値 | 始値→エントリーの上乗せ |
|---|---|---|---|---|
| 8/19 | 9278 | **+1.81%** | 3.32% | +1.46% |
| 8/20 | 7513 | **+1.06%** | 3.05% | +2.04% |
| 8/21 | 9278 | **+0.98%** | 3.50% | +2.50%（**エントリー=当日高値**） |

**システムは「始値から平均+2%上乗せした位置」で買っている。** これは記述的事実。
ただし「寄り付きで買えば勝てる」という結論は**導けない**（反実仮想に未来情報が入っているため）。

**判定ロジックは変えない。記録だけ増やす。** 20件揃った時点で層別比較する。

### 2.4 既知の未修正 issue

| # | 内容 | 影響 / 優先度 |
|---|---|---|
| 1 | `CalcPositionSize` が `Int(rawShares/100)*100` | 株価5,000円超で0株。コア30最高値3,080円なので現状は表面化しない |
| 2 | 同上の切り捨て | 実リスクが 1% → 0.57%（8/19）/ 0.65%（8/21）に目減り |
| 3 | 監視窓が10:00まで | ザラ場型を拾う。設計通り。二重集計ルールで対処 |
| 6 | Trades V列ヘッダ名の不一致（コード「発火区分」/ 実物「パターン区分」） | 実害なし |
| 7 | ランナー保持に上限時間がない | 含み益1.5%超なら15:00まで保持 |
| 8 | **追跡チェーンの保護がメモリ依存** | VBA状態リセットで Watchdog ごと死ぬ。`[L]` で事後検知のみ |
| 9 | `[H]` はシグナル後にメモリが死んだ日を記録できない | 8/19がその状態 |
| 10 | **`ギャップ率` が始値ベースでない** | §2.3。3件/3件で確認済み。20件時に層別評価 |
| 12 | `FinalizeTracking` が価格を読まずに確定する | **B13=15:31 で実害は封じられた（8/21実測）。優先度：低** |
| 14 | **SS_MMDD がシートモジュールを毎営業日1本増やす** | 8/21時点で Sheet15。20件到達時に Sheet50近く |

（#4 #5 #11 #13 は解消済み。#13 は「決算発表日は過去日付しか返さない」→「3割しか埋まっていない」に訂正）

### 2.5 直近の優先タスク（TODO）

```
P0 ── リポジトリの取りこぼし解消（Claude Code だけで完結する）
  [ ] 1. src/modPosition.bas を xlsm 実物（890行）から書き出して push
        └ Shift-JIS(CP932)/CRLF。Attribute VB_Name 行は残す（.bas は Import 用）
  [ ] 2. data/trades.csv に 8/21 の 9278 行を追加（Trades 7行目から生成）
  [ ] 3. STATE.md の「実質バックアップが存在しない状態」段落を削除（§2.0-B の通り事実誤認）
  [ ] 4. tools/ に監査スクリプトを作成（§4.2）— 毎回手書きしているのを固定資産化する

P1 ── 週末バックテスト（8/22-23、非取引日限定）
  [ ] 5. modDaily7 の BASE_DATE を 2026/08/08 → 直近営業日に更新
  [ ] 6. RssChartPast で 200本×30銘柄＝6,000銘柄日を取得
        └ 目的：真の寄り付きギャップ3%以上の日を抽出し、
           (高値-始値)/始値 ≧ +3% → 利確、(安値-始値)/始値 ≦ -2% → 損切 で勝敗判定
        └ 8/20 の「安値=始値だから損切り不可能→利確確定」のような
           順序非依存の判定は日足だけで厳密にできる（8/21に実証）
        └ 限界：高値と安値のどちらが先かは日足では分からない。順序依存ケースは解けない
        └ 限界：これは「寄り付きエントリー」の検証。現行の「時点騰落率エントリー」の検証ではない
  [ ] 7. 8/10(4813) / 8/13(7322) / 8/14(8550) の始値を RssChartPast で確定し、
         真の寄り付きギャップを算出 → Trades U列に追記

P2 ── 継続運用
  [ ] 8. [N] の実動作確認（次のシグナル日、決済記録で番号選択が出るか）
  [ ] 9. 紙トレード継続。寄り付き型 20件が目標（残り17件・約40営業日 ≒ 10月下旬）
  [ ] 10. 毎日引け後のスナップショット（退避 → (1)取得 → (2)数式固定 → 保存）
  [ ] 11. 20件到達後、M/N列と O/P列から最適TP/SLを逆算。真のギャップでも層別する
```

### 2.6 「やらないことリスト」— 提案する前に必ず確認する

**以下は既に検討して却下・保留した案。Claude Code から再提案しないこと。**

| 案 | 判断 | 理由 |
|---|---|---|
| B2（損切り率）を緩める | **却下** | 8/21 は損切りが正しく機能した例。緩めれば -3.90% まで引っ張っていた |
| B3 を +4% に戻す | 却下 | n=6 で判断するのは早計 |
| シグナル条件を始値ベースに変更 | **却下（今は）** | 20件ルール。記録は増やすが判定は変えない |
| 「寄り付きで入れば勝てる」と結論する | **却下** | 反実仮想に未来情報が入っている。実装不可能 |
| 「1日1回だけ発火」ルールを外す | 却下 | 8/20 の松屋は寄り付き+1.32%でそもそも対象外 |
| 境界時刻を 9:05 に緩める | 却下 | 唯一の根拠が「件数が集まりやすい」 |
| 「時間切れ」を「ランナー失格」に改名 | **却下（今は）** | 既存3行と混在する。改名するなら全行一括で |
| 決済理由を自由入力に戻す | 却下 | `[N]` で塞いだ。表記ゆれは集計時に効く |
| Watchlist に始値列を追加 | **不要** | スナップショットが既に始値を取得している |
| Watchlist を増やす | 保留 | コア30の選定基準を緩めるとサンプルの均質性が崩れる |
| Python + J-Quants へ移行 | 見送り | RssChartPast で日足が取れる |
| 全3,117銘柄の再スクリーニング | 見送り | 紙トレ検証が先 |
| イベント銘柄の自動抽出 | 凍結 | 決算発表日が3割しか埋まらない（除外フィルタとしては有効） |
| 8/10 の決済時刻・保有時間を推定値で補完 | 却下 | 推定値混入のリスクのほうが大きい |
| 8/19 の O5/P5 を補完 | 却下 | 経路情報は終値から復元できない |
| 無人日の監視起動 | 却下 | エントリー記録ができずデータにならない |
| `TestCheckGapSignalsNow` の実行 | 非推奨 | Log に偽シグナルが混入する |
| CommandBars 方式の UI | 却下 | Top/Left が (-9,-9) 固定。UserForm で解決済み |
| Position の古い行を手で消す | 却下 | 次のエントリーで自動上書きされる |
| STATE.md の「実装済み」記述を信じる | **却下** | xlsm 実物を監査してから判断する |
| 1日に2件のコード変更をまとめて入れる | **却下** | 不具合が出たとき切り分け不能 |
| 取引日の朝にコードを触る | **却下** | 8/12 の再演リスク。変更は必ず引け後 |

---

## 3. コーディング規約 & 設計原則

### 3.1 命名規則

| 対象 | 規則 | 例 |
|---|---|---|
| 標準モジュール | `mod` + PascalCase | `modPosition` |
| UserForm | `frm` + PascalCase | `frmController` |
| Public 変数（Config由来） | PascalCase・型を接尾しない | `GapThresholdPct` `TrackEndTime` |
| モジュール変数 | `m` + PascalCase | `mIsRunning` `mTrackEntryPrice` |
| 定数（シート名） | `SHEET_` + 大文字 | `SHEET_TRADES` |
| 定数（列番号） | `COL_`（Watchlist） / `PCOL_`（Position） | `COL_GAPPCT` `PCOL_TRACK` |
| 定数（閾値） | 大文字スネーク | `STALE_LIMIT_SEC` `TRACK_INTERVAL_SEC` |
| 施策の識別子 | `[A]`〜`[N]` の連番 | コメント・STATE.md・コミットメッセージで共通 |

### 3.2 ファイル分割ルール

- **1モジュール = 1責務**（監視 / ポジション / 設定 / 通知 / スクリーニング / 統計 / UI / 監視役）
- **`modConfig` は全モジュールから参照される定数の集積地。** 他モジュールに定数を散らさない
- **モジュール冒頭に改訂履歴コメントを必ず置く。** 日付＋施策記号＋「なぜ変えたか」。
  現行コードはこの慣習を守っている（`modMonitor` は 8/13〜8/19 の5回分が残っている）。**これを崩さない**

### 3.3 エラーハンドリング方針 — 最重要の掟

> ### `On Error Resume Next` を書くときは、握りつぶした先が安全側かを必ず確認する。

`[M]` のバグはこれが原因で**数週間不可視**だった。
`GetConfigTime` が `TimeValue(Double)` の型エラー13 を `On Error Resume Next` で飲み込み、
**警告ゼロで既定値に落ちていた**。B5/B6/B7 はシート値と既定値が同一だったため永久に観測不能で、
B13 を 15:31 に変えて初めて露見した。

**具体ルール**

| ルール | 内容 |
|---|---|
| 適用範囲を最小化 | `On Error Resume Next` は必要な1〜数行に限定し、直後に `On Error GoTo 0` |
| フォールバック値を疑う | 既定値に落ちる分岐を書いたら「落ちたことが観測可能か」を必ず自問する |
| 観測不能な設定を作らない | シート値と既定値が同じなら、そのバグは**永久に発見できない** |
| チェーンは先に予約 | `MsgBox` の**前**に `ScheduleNextCheck` を呼ぶ（`[D]`）。UI で止まってもチェーンは死なない |
| 連続エラーで停止 | `MAX_CONSECUTIVE_ERRORS = 5` で監視自体を止める |
| 古い値を残さない | 値が取れない銘柄は `ClearContents`（`[E]`）。前日の数値が居座ると当日値と区別できない |

### 3.4 変更手順の規律（破ると事故る）

| ルール | 理由 |
|---|---|
| **取引日の朝にコードを触らない** | 8/12 に監視が動かず1日分のデータを失った |
| **1日に入れるコード変更は1件まで** | 不具合が出たとき切り分け不能になる |
| **場中に VBE を開かない** | Public/モジュール変数がリセットされ、Watchdog と追跡チェーンが同時に死ぬ |
| **同名モジュールは「解放（削除）→ Import File」** | そのまま Import すると `modConfig1` が増え、**元のコードが生き続ける** |
| **ドキュメントモジュール（ThisWorkbook / Sheet系）は Import 不可** | コードペインを全選択→削除→貼り付け。`Attribute VB_...` 行は貼らない。<br>成功判定：VBE のドロップダウンが `(General)` → `Workbook` / `Open` に変わる |
| **`.bas` は Shift-JIS(CP932) / CRLF** | UTF-8 だと VBE で文字化けする。**Claude Code が書き出すときの既定は UTF-8 なので必ず変換する** |
| **Config を変更したら `ShowLoadedConfig` で目視** | 読み込まれた実値を確認する唯一の手段 |
| **15:31 を過ぎるまで引け後の操作を始めない** | `FinalizeTracking` は 15:31 直後のティックで発火する |

### 3.5 テスト方針

**自動テストは存在しない。** 単体テストフレームワークも CI もない。代わりに以下の規律で担保する。

| レベル | 方法 |
|---|---|
| **静的監査** | Python で xlsm を読み、モジュール本数・Config実値・数式残存・Trades整合を毎回確認（§4.2） |
| **設定値の検証** | `ShowLoadedConfig`（Alt+F8）で読込結果を全項目 MsgBox 表示 |
| **単体の手動確認** | `modDaily7.TestOneStock`（1銘柄だけ完走させて数値を目視） |
| **実動作検証** | **本番のシグナル日にしか取れない。** 「実装済み ✅」と「実動作検証 ✅」を STATE.md の表で**必ず分けて管理する** |
| **回帰の検算** | 差し替え前後で同一の問いをイミディエイトウィンドウに投げ、値を突き合わせる（`[M]` でこれをやった） |
| **禁止** | `TestCheckGapSignalsNow` の実行（Log に偽シグナルが混入して実データが汚れる） |

**Claude Code に期待するテスト貢献**：VBA を直接実行できない以上、
**「xlsm 実物 vs src/*.bas の差分検出」と「Trades / Log / Position の整合性検査」をスクリプト化する**のが最大の価値。

### 3.6 ドキュメント規約

| ファイル | 運用 |
|---|---|
| `STATE.md` | **毎回まるごと上書き**。現在地だけを書く。過去の経緯は書かない |
| `KNOWLEDGE.md` | **追記のみ・削除しない**。検証レベルを必ず付ける：**【公式】/【実測】/【推測】** |
| `Trades` U列 | **恒久的に残すべき注記はここ**。STATE.md は消えるが Trades は残る |
| コミットメッセージ | Conventional Commits 風（`fix(config): ...`）＋日本語本文で「なぜ」を書く |

---

## 4. 実行・ビルド・テスト用コマンド

### 4.1 ビルドは存在しない

**このプロジェクトにビルド工程はない。** `npm` も `make` も `pytest` もない。
「実行」＝ Windows 上で Excel を開いて操作パネルのボタンを押すこと。**Claude Code からは実行できない。**

<details>
<summary>本番の実行手順（ユーザーが Windows 上で手動実施。Claude は指示のみ）</summary>

**朝（取引日）**
1. マーケットスピードII を起動してログイン（**Excel より先に**。逆だと数式が `#N/A` で固まる）
2. `デイトレ用.xlsm` を開く（マクロ有効化 → 操作パネルが自動表示）
3. 「大引け追跡が未完走です」の警告が出たら、前日 Trades 行の O/P列を確認して U列に注記
4. Watchlist C:F と Position E2 に RSS数式があるか確認
5. **8:55** に操作パネルの「監視開始」を押す
   - パネルが出ていない場合：`Alt+F8` → `ShowController`

**引け後（15:32 以降。15:31 まではシートを触らない）**
1. Position **T2** を確認（`追跡完了 15:31:xx` なら正常）
2. Trades 当日行の **O / P / Q** を確認
3. `スナップショット` シートを **`SS_MMDD`（中身のデータの日付＝前営業日）** にリネーム
4. 操作パネル **(1)スナップショット取得** →「取得完了：30銘柄」
5. 操作パネル **(2)数式固定** →「値に固定しました」（**やらないと次回オープン時に RSS 再登録**）
6. 上書き保存
7. シグナルがあった日は Trades U列に注記を追記

⚠️ 手順4の実行中、RSS登録は **Watchlist 120 ＋ Position 1 ＋ スナップショット 360 = 481**（上限500）。
</details>

### 4.2 監査コマンド（Claude Code の主戦場）

> **`tools/` は未作成。P0-4 で作ること。** 以下は仕様と、それまでのワンライナー代替。

```bash
# 依存（初回のみ）
pip install openpyxl oletools --break-system-packages

# --- ① VBA を xlsm から書き出す（src/ 更新の唯一の正しい経路） -------------
#   仕様: tools/export_vba.py <xlsm> <outdir>
#   - olevba で全モジュールを抽出
#   - .bas は Attribute VB_Name 行を残す（Import 用）
#   - .cls は Attribute 行を除去（貼り付け専用のため）
#   - 出力は必ず cp932 / CRLF
python3 - <<'PY'
from oletools.olevba import VBA_Parser
import os
p = VBA_Parser('デイトレ用.xlsm'); os.makedirs('src', exist_ok=True)
for _, _, name, code in p.extract_macros():
    if name.startswith('Sheet') or name.startswith('frmController'): continue
    if name.endswith('.cls'):
        code = '\n'.join(l for l in code.splitlines() if not l.startswith('Attribute '))
    open('src/'+name, 'w', encoding='cp932', newline='\r\n').write(code)
PY

# --- ② src/*.bas と xlsm 実物の乖離検出（★最重要★ 今回これで [N] 未push を発見） ---
#   仕様: tools/diff_vba.py → モジュールごとに SAME / DIFF を出力し、DIFF があれば exit 1
#   実行タイミング: 毎コミット前

# --- ③ xlsm 構造の健全性チェック --------------------------------------------
#   仕様: tools/audit_xlsm.py → 以下を検査し、逸脱を列挙
#     - 標準モジュールがちょうど9本か（modConfig1 等の混入検出）
#     - Config B1..B14 の実値
#     - 残存 RSS数式の総数（期待値 121。スナップショット系は0）
#     - Trades の行数・K列の語彙（利確/損切/時間切れ/引け前強制/手動判断 の5種のみ）
#     - Trades ⇔ Log ⇔ Position の日付・銘柄・損益率の整合
python3 -c "
import openpyxl
wb = openpyxl.load_workbook('デイトレ用.xlsm', data_only=True)
print({r[0].value: r[1].value for r in wb['Config'].iter_rows(min_row=1, max_row=14)})
"

# --- ④ Trades → data/trades.csv 書き出し ------------------------------------
#   仕様: tools/export_trades.py
#   - 22列すべて / 日付は YYYY-MM-DD / 時刻は HH:MM:SS / 改行 CRLF / BOM付きUTF-8
#     （既存 trades.csv が BOM付き CRLF なので踏襲する）

# --- ⑤ エンコーディング Lint（.bas は CP932/CRLF 必須） -----------------------
for f in src/*.bas src/*.cls; do
  iconv -f CP932 -t UTF-8 "$f" >/dev/null 2>&1 || echo "NG(not cp932): $f"
  file "$f" | grep -q CRLF || echo "NG(not CRLF): $f"
done
```

### 4.3 git 運用

```bash
# 取引日の終了後
git add STATE.md data/trades.csv && git commit -m "docs(state): MM/DD の記録" && git push

# VBA を変更した直後（変更したモジュールのみでよい）
python3 tools/export_vba.py && python3 tools/diff_vba.py   # DIFF が消えることを確認
git add src/modXxx.bas && git commit -m "fix(xxx): ..." && git push

# 週1回（バイナリバックアップ。UserForm を復元できる唯一の手段）
git add デイトレ用.xlsm && git commit -m "chore: xlsm backup" && git push
```

**コミットメッセージ規約**

```
<type>(<scope>): <日本語の1行要約>

何が起きていたか（症状）。
なぜそうなっていたか（原因）。
どう直したか（対策）。

- 変更点1
- 変更点2

あわせて記録した内容（トレード記録・検証完了・issue訂正など）。
```

`type` は `fix` / `feat` / `docs` / `chore` / `refactor`。`scope` はモジュール名（`config` `position` `monitor` `state`）。

---

## 付録：Claude Code へのふるまい指示

1. **STATE.md を鵜呑みにしない。** 何かを判断する前に xlsm を Python で読んで確かめ、
   **ドキュメントとの乖離を見つけたら黙って直さず、必ず報告する。**
2. **完全な置き換えファイルを出す。** 差分パッチや「ここに手で挿入してください」形式は使わない。
3. **手順は1ステップずつ。** 実行可能な作業（コード生成・集計・監査）は Claude 側で完結させる。
4. **20件ルールを守る。** パラメータ変更の提案はしない。「記録は増やすが判定は変えない」。
5. **1件の結果から規律を緩める提案をしない。** 損切りを緩める・閾値を下げる等は却下済み（§2.6）。
6. **`.bas` を書き出すときは必ず CP932 / CRLF。** UTF-8 のまま渡すと VBE で文字化けする。
7. **VBA を直したら、必ず「解放 → Import」または「コードペイン貼り付け」の反映手順をセットで示す。**
