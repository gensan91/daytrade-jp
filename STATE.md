# STATE — デイトレ自動化プロジェクト 現在地

最終更新：2026/8/20（木）引け後 ＋ 監査反映／このファイルは毎回上書き。技術知見は `KNOWLEDGE.md` へ。

---

## まず最初にやること

1. マーケットスピードII を起動してログイン（**Excelより先に**。逆だと数式が `#N/A` で固まる）
2. デイトレ用.xlsm を開く（マクロ有効化 → 自動でデイトレ操作パネルが表示される）
3. **「大引け追跡が未完走です」の警告が出たら**、前日の Trades 行の O/P列を確認して U列に注記する
   （※ 8/20は追跡が完走したので Position T2 は `追跡完了 15:30:14`。**8/21朝は警告が出ないのが正常**）
4. Watchlist C:F に RSS数式が入っているか確認（`=RssMarket($A2,"項目名")`／30行＝120セル）
5. Position E2 に `=IF($A2="","",RssMarket($A2,"現在値"))` が入っているか確認
6. 8:55 に 操作パネルの「監視開始」ボタンを押す

操作パネルが出ていない場合： Alt+F8 → `ShowController` を実行。

## 引け後にやること（15:31以降）

1. スナップショットシートをコピーして **`SS_MMDD`**（＝**そこに入っている前日データの日付**）にリネームして退避
   （**`ws.Cells.Clear` されるため必須**）
2. 操作パネル **(1)スナップショット取得** → 「取得完了：30銘柄」
3. 操作パネル **(2)数式固定** → 「値に固定しました」（**やらないと次回オープン時にRSS再登録**）
4. 上書き保存
5. シグナルがあった日は Trades の U列に注記を追記
6. **GitHub へコミット**（`STATE.md` / `data/trades.csv` / `デイトレ用.xlsm`）

> **Config を触ったら必ず Alt+F8 → `ShowLoadedConfig` を実行して、
> 実際に読み込まれた値を目視する。**（8/20 追加）

---

## 🔴 8/20（監査で判明）：`Q6 = +1.17%` であって +1.10% ではない。8/20 も終値を取り逃していた

前版の STATE.md は「8/20 はオークションから14秒後に確定したため結果的に終値を拾えた公算が大きい」と
書いていた。**これは誤り。** 原理的に拾えない。

| | 値 | 逆算価格 |
|---|---|---|
| Trades **Q6 実測** | **1.17%** | 1454 × 1.0117 = **1,471円** |
| スナップショット 7513 現在値（＝当日終値） | — | **1,470円** |
| O6 大引けMFE 1.99% | 高値 1,483 ÷ 1454 = +1.99% ✅ | 追跡値そのものは正確 |

### 原因（コードで確定）

`modPosition.TrackAfterExit` の冒頭：

```vba
If TimeValue(Now) >= TrackEndTime Then
    FinalizeTracking      ' ← 価格を一切読まない
    Exit Sub
End If
' ↓ 価格の読み取りはこの後にある
```

**`FinalizeTracking` は値を読まない。** よって Q列に入るのは常に
**「TrackEndTime 未満で回った最後のサイクルの値」**。`TRACK_INTERVAL_SEC = 15`。

### タイムスタンプで裏取り済み

追跡は決済時刻から15秒周期で回るため、読み取り秒は決済時刻の秒に固定される。

| 日 | 決済時刻 | 周期の秒 | 最後の読み取り | T列（打ち切り） |
|---|---|---|---|---|
| 8/14 | 9:25:16 | :01 / :16 / :31 / :46 | **15:29:47** | 追跡完了 15:30:02 ✅ |
| 8/20 | 9:19:59 | :14 / :29 / :44 / :59 | **15:29:59** | 追跡完了 15:30:14 ✅ |

**8/14 と 8/20 はまったく同じ構造で終値を逃している。** 差が1円だったのは偶然にすぎない。

### 🟢 ただし B13=15:31 はこの構造を正しく直す

TrackEndTime=15:31 なら 15:30:14 / :29 / :44 / :59 の読み取りが**通る**ので、
最後の読みがクロージング・オークション（15:30:00執行）**後**になる。

**次回シグナル日の検証項目を1つ増やす：**

- 旧：「T2 が 15:31台で `追跡完了` になるか」
- 新：**「Q列の逆算価格（エントリー価格 × (1+Q/100)）が MS2 の終値と一致するか」** ← こちらが本命

---

## 🔴 8/20 も真の寄り付きギャップ3%以上は **0件**

8/20スナップショット30銘柄から算出（始値 ÷ 前日終値 − 1）。

| 順位 | 銘柄 | 前日終値 | 始値 | 寄り付きギャップ | 終値ベース |
|---|---|---|---|---|---|
| 1 | 9505 北陸電力 | 963.6 | 986 | **+2.32%** | +2.18% |
| 2 | 9418 U-NEXT HD | 1744 | 1779 | +2.01% | +1.78% |
| 3 | 6727 ワコム | 772 | 786 | +1.81% | +3.63% |
| 4 | 6023 ダイハツインフィニアース | 2854 | 2904 | +1.75% | +4.38% |
| 5 | 5032 ANYCOLOR | 2764 | 2810 | +1.66% | +3.11% |
| … | **7513 コジマ** | 1410 | **1425** | **+1.06%** | +4.26% |

**検証可能な2日（8/19・8/20）が2日とも 0/30。それでもシステムは2日とも発火した。**

※ 8/19 のランキング（下記）は `SS_0819` から再計算して**全数一致**を確認済み。あの表は信用してよい。

| 順位 | 銘柄 | 寄り付きギャップ | 終値ベース |
|---|---|---|---|
| 1 | 9418 U-NEXT HD | +2.26% | +0.87% |
| 2 | 8237 松屋 | +1.81% | +6.90% |
| 2 | **9278 ブックオフGHD** | **+1.81%** | +3.61% |
| 4 | 5032 ANYCOLOR | +1.07% | -1.07% |

補足：コジマは **安値1425 ＝ 始値1425（寄り底）**、始値→高値 +4.07%。
「寄ってから買えば全部取れた」形状であり、寄り付き需給の不均衡とは無関係。

### 検証可能な全件の状況

| 日付 | 銘柄 | 前日終値 | 始値 | 真の寄り付きギャップ | システム判定値 | 検知 |
|---|---|---|---|---|---|---|
| 8/10 | 4813 | — | — | **未確認** | 3.15% | 9:00:16 |
| 8/13 | 7322 | — | — | **未確認** | 3.08% | 9:00:44 |
| 8/14 | 8550 | — | — | **未確認** | 3.06% | 9:08:17 |
| 8/19 | 9278 | 2,767 | 2,817 | **+1.81%** ❌ | 3.32% | 9:00:13 |
| 8/20 | 7513 | 1,410 | 1,425 | **+1.06%** ❌ | 3.05% | 9:01:15 |

**検証できた2件が2件とも真のギャップアップではなかった。** 主サンプル「寄り付き型3件」のうち
真のギャップアップが何件かは、8/10・8/13 の始値を取るまで確定しない。

---

## 🟢 8/20（監査）：ベースレートは**すでにワークブックの中にあった**

`modDaily7.Aggregate` の集計式：

```vba
gap = Abs(op - prevCl) / prevCl * 100    ' op = 始値
```

**これは真の寄り付きギャップ（絶対値・ギャップダウン込み）。**
そして `日足集計` シートに、コア30の20日ぶんの「ギャップ3%以上の日数」が既に入っている。

| | 値 |
|---|---|
| コア30の観測 | **600 銘柄日** |
| \|gap\| ≧ 3% | **31日（5.17%）** |
| \|gap\| ≧ 5% | 11日（1.83%） |
| 30銘柄での期待件数 | **1.55件/日**（上方向のみなら概ね **0.78件/日**） |

### ⚠️ この数字には選択バイアスがある

コア30のスコア式は `+ 0.20 × z(ギャップ3%以上の日数)` を含む。
**つまり「ギャップが出やすい20日だった銘柄」を選んだ結果を、同じ20日で測っている。**
前向きの期待値としては上振れしている。

`KNOWLEDGE.md §7` の母集団側の実測（151銘柄×20日）は 3%以上が **3.64%**。
したがって前向きの現実的なレンジは：

| | \|gap\|≧3% の発生率 | 30銘柄での件数/日 | 上方向のみ |
|---|---|---|---|
| 母集団（151銘柄） | 3.64% | 1.09件 | 約0.55件 |
| コア30（同一20日・上振れ） | 5.17% | 1.55件 | 約0.78件 |

### ここから言えること

1. **始値ベースに判定を変えてもシグナル頻度はほぼ落ちない。**
   現行の発火は6営業日で5件＝0.83件/日。上表の上方向レンジ 0.55〜0.78件/日 とほぼ同じ。
   1日1回ルールがあるので実質0.5〜0.6日/件。**20件到達までの日数は変わらない。**
   → 「判定は変えない」判断は維持するが、**根拠は「比較対象が消えるから」の一本のみ**になった。
   　「頻度が落ちるから」は成り立たない。
2. **8/19・8/20 が0件だったのは異常ではない。**
   上方向のみ p≈2.6% なら P(30銘柄で0件)≈45%、2日連続で約20%。
   **サンプル不足であってモデル崩壊ではない。**
3. コア30内で |gap|≧3% を最も出したのは **1407（3日）**、次いで 6023 / 8367 / 6817 / 8923 / 9743 / **7513**（各2日）。
   一方 **8237 松屋・8628 松井証券など6銘柄は20日間で0日**。
   **値幅率で選んだコア30は、寄り付きギャップの出やすさとは別の軸で並んでいる。**

---

## 🟢 8/10・8/13・8/14 の始値は MS2チャート手読み不要

`RssChartPast` で取れる（`KNOWLEDGE.md §4` に仕様が確定済み。列6＝始値）。
空きシートに数式を1本置くだけでよい。

```
=RssChartPast(,"4813.T","D",20260801,20)
```

3銘柄（4813 / 7322 / 8550）で3本。始値と前日終値が同時に取れる。
さらにコア30全部に40本ぶん投げれば、**始値の日次記録を過去に遡って作れる。**

- ⚠️ **`BuildDailyStats` は絶対に回さないこと。** `BASE_DATE = "2026/08/08"` がハードコードされており、
  `日足集計` と `選別候補` が `Cells.Clear` される。**上のベースレートの一次データが消える。**
  手打ち数式か、別シートに書く新規プロシージャで行う
- データは数式セルの**2行下**から。末尾に `--------` 行が入る

---

## ✅ 8/20 に検証完了：`[H]` 日次サマリ

Log 15行目に初めて日次サマリ行が出た。

```
2026/8/20 10:00 日次サマリ 8237 松屋 8.29 シグナルあり
監視 08:59-10:00／最終チェック 10:00:00／E列はセッション中の最大ギャップ率（G列の終了時点値ではない）
```

### ⚠️ 日次サマリE列の読み方（重要）

`mMaxGapPct` は毎サイクル無条件に更新されるため、**E列は時刻を問わない値**。
9:00の板寄せ由来か、9:45のトレンド由来かを区別しない。

**この列を「寄り付き型の機会指標」として読むと必ず誤読する。**
8/20 は実際に誤読しかけた（松屋8.29%を「取り逃した寄り付きギャップ」と誤認 → 始値+1.32%で否定）。

---

## 8/20 の結果：ザラ場型2件目

| 項目 | 値 |
|---|---|
| 銘柄 | 7513 コジマ |
| シグナル検知／エントリー | 09:01:15 @ 1,454／300株（記録操作 09:02:14） |
| 決済 | 09:19:59 @ 1,442 |
| 決済理由 | 時間切れ（＝実質ランナー失格） |
| 損益率 | **-0.83%** |
| 保有中MFE / MAE | **-0.62%** / -1.17% |
| 大引けMFE / MAE | +1.99% / -1.17%（高値1,483で裏取り済み） |
| **Q6（終値ベース損益率）** | **+1.17% ＝ 15:29:59時点 1,471円。当日終値ではない** |
| 真の終値ベース | 終値1,470 → エントリー比 **+1.10%**（スナップショットから算出） |
| コスト後 | **約 -5,345円**（gross -3,600 − 436,200×0.4%） |
| 区分 | **ザラ場型**（V6で自動判定を確認済み） |

エントリーが 9:01:15 になったのは、**コジマの寄り付きが9:01過ぎまでずれ込んだ**ため。
`current > 0` になった最初のサイクルが 9:01:15 だった。
**境界時刻ルールは「板が立つのが遅い銘柄を自動的にザラ場型へ回す」副次効果を持っていた。**

---

## 新しい観測軸：決済後に上がる

| 日付 | 銘柄 | 決済 | 終値（エントリー比） | 出所 | 決済後の差 |
|---|---|---|---|---|---|
| 8/19 | 9278 | -2.38% 損切 | +0.31% | Q5＝終値2,867から手動算出。`SS_0819` で2,867を裏取り済み | **+2.7pt** |
| 8/20 | 7513 | -0.83% 失格 | +1.10% | **スナップショット終値1,470から算出。Q6(1.17%)ではない** | **+1.9pt** |

2件連続で、決済した後に上昇している。`大引けMFE` が `保有中MFE` を大きく上回る傾向が
20件で続くなら、**利確・時間切れが早すぎる**という仮説の材料になる。**現時点では n=2。記録するだけ。**

### 寄り天率（寄り付き型のみ）

寄り付き型3件の保有中MFE は +3.00% / +2.08% / **-0.38%**。3件中1件が寄り天。
※ 8/20 のコジマ（保有中MFE -0.62%）は**ザラ場型なので主サンプルに含めない**。

---

## 施策の実装・検証状況

| 対策 | 実装 | 実動作検証 |
|---|---|---|
| `[G]` Watchdog誤検知 | — | ✅ クローズ（8/18以降 復旧ログ0件） |
| `[H]` modMonitor 日次サマリ | ✅ | ✅ **8/20 検証完了** |
| `[J]` modPosition Q列確定 | ✅ | ✅ **完了**。未完走時に空欄＝8/19／完走時に埋まる＝8/20（Q6=1.17を目視）。**ただし書かれる値は終値ではない → issue#12** |
| `[K]` modSnapshot 完全再計算 | ✅ | ✅ **8/20 検証完了**（30銘柄すべて空セルなし・数式0件） |
| `[L]` ThisWorkbook 起動時警告 | ✅ | ✅ 8/19 検証完了 |
| `[M]` modConfig 時刻読込修正 | ✅ | ✅ **8/20 検証完了**（イミディエイトで前後比較） |
| B13=15:31 の実効果 | ✅ | ❌ **未検証**。次のシグナル日に **Q列の逆算価格＝MS2終値** で確認する |

---

## 現在の状態

| 項目 | 状態 |
|---|---|
| コア30銘柄 | ✅ 確定 |
| Config B1（ギャップ率閾値） | ✅ 3% |
| Config B2（損切り率） | ✅ -2% |
| Config B3（利確率） | ✅ 3% |
| Config B13（終値追跡終了時刻） | ✅ 15:31（**8/20に実効化。効果は未検証**） |
| Config B14（往復コスト率） | ✅ 0.4 |
| Watchlist RSS数式 | ✅ 投入済み（C:F 30行＝120セル） |
| Position E2 | ✅ `=IF($A2="","",RssMarket($A2,"現在値"))` |
| Trades シート（恒久履歴・22列） | ✅ 稼働中。U2〜U6 記入済み |
| modWatchdog | ✅ 稼働。ただし**追跡チェーンの保護はメモリ依存で限界あり** |
| `ShowLoadedConfig` | ✅ 稼働（Config変更時の必須確認手段） |
| スナップショット（始値の日次記録） | ✅ **8/20 から毎日運用開始**。`SS_0819` に前日分を退避済み |
| **GitHub 同期** | ✅ **8/20 に是正**。それまで README/STATE/.gitignore の3ファイルのみ、STATE は8/17版だった |
| 紙トレード | 🟡 有効5件 / 20件（寄り付き型**3**／ザラ場型**2**） |
| イベント銘柄 | ❌ 未実装（RSS「決算発表日」が構造的に使えないため凍結） |

---

## トレード記録（Trades シート＝唯一の正）

| 日付 | 銘柄 | 検知 | エントリー | 決済 | 理由 | 損益率 | コスト後 | 区分 |
|---|---|---|---|---|---|---|---|---|
| 8/10 | 4813 ＡＣＣＥＳＳ | 9:00:16 | 393 | 404.8 | 利確 | +3.00% | +12,274円 | 寄り付き型 |
| 8/13 | 7322 三十三FG | 9:00:44 | 1975 | 1992 | ランナー失格 | +0.86% | +1,820円 | 寄り付き型 |
| 8/14 | 8550 栃木銀行 | 9:08:17 | 1115 | 1132 | ランナー失格 | +1.52% | +5,016円 | ザラ場型 |
| 8/19 | 9278 ブックオフGHD | 9:00:13 | 2858 | 2790 | 損切 | -2.38% | -7,943円 | 寄り付き型 |
| 8/20 | 7513 コジマ | 9:01:15 | 1454 | 1442 | ランナー失格 | -0.83% | 約-5,345円 | ザラ場型 |

**3勝2敗／コスト後累計 約 +5,822円**

8/12 は監視の稼働状態に疑義があり、検証件数にカウントしない（Log に記録済み）。
8/18 はシグナルなし（`[H]` 実装前のため Log 側に痕跡なし。STATE.md のみが根拠）。

### データ純度に関する取り決め

- **8/10**：決済価格は +3.00% からの逆算値。決済時刻（I2）・保有時間（R2）は推定になるため空欄。
  終値ベース損益率（Q2）も由来不明のため削除済み。U2 の注記が根拠
- **8/13**：旧コード期のため大引け追跡（O/P列）が存在しない。Q3 は Log に `+3.91%` の記載があるが
  由来を追えないため採用しない。**基準の混在したサンプル**（価格は気配値1975、時刻は9:07:35）で
  記録開始時点で既に +0.86%。20件集計時は外れ値として扱うか感度分析で除外する
- **8/14 の Q4（-0.45%）は「終値」ではなく 15:29:47 時点の値。** B13が実質15:30だったため
  15:30 のクロージング・オークションを取り逃している。根拠は Position T2「追跡完了 15:30:02」
- **8/19 の O5/P5 は 10:40:25 までの値。** Q5（+0.31%）は終値2,867からの手動算出。
  `SS_0819` の 9278 現在値 = 2,867 で裏取り済み
- **8/19 の実損切は -2.38%（ライン -2%）。0.38%のスリッページ**（3秒ポーリング＋手動発注のラグ）
- **8/20 の Q6（+1.17%）は「終値」ではなく 15:29:59 時点の 1,471円。**
  真の終値は 1,470円（スナップショット）＝ エントリー比 **+1.10%**。
  8/14 とまったく同じ構造の取り逃し（issue#12）。O6/P6 も同じ時刻で打ち切られている
- **全件に共通：判定値は「寄り付きギャップ」ではなく「検知時点の前日終値比騰落率」**（issue#10）

> 上記の取り決めは **Trades U列にも同内容を書いている**（8/19夜〜）。
> STATE.md は上書き運用なので、20件集計時に生き残っている保証がないため。

---

## サンプルの二重集計ルール（8/14 制定）

| 区分 | 定義 | 位置づけ | 現在の件数 |
|---|---|---|---|
| 寄り付き型 | 検知時刻 ≦ 9:01:00。板寄せの需給不均衡が源泉 | 主サンプル。目標20件 | **3件** |
| ザラ場型 | 検知時刻 > 9:01:00。継続中のトレンドが源泉 | 並行記録。別集計 | **2件** |

- 最適TP/SLの逆算は**寄り付き型20件が揃ってから、寄り付き型のみで**行う
- **どちらの区分でも、20件到達までパラメータは一切変更しない**
- **⚠️ この境界が「寄り付きギャップ」の代理として機能するかは、検証できた2件が2件とも否定している。**
  始値の記録が貯まってから再評価する

### 進捗の見積もり（8/20時点）

有効観測日6日で寄り付き型3件 → 0.5件/日。残り17件で**約34営業日 ≒ 10月中旬**。
n=3 なので信頼区間は極めて広い。

---

## 次にやること（優先順）

1. **Trades U6 に注記を追記**（下記の文面をそのまま貼る）
   > `Q6=1.17%は15:29:59時点の1,471円。当日終値1,470円ではない（真の終値ベースは+1.10%）。FinalizeTrackingは価格を読まない仕様のため、Q列は常にTrackEndTime直前の読み取り値。8/14 Q4と同一構造。issue#12`
2. `RssChartPast` で **4813 / 7322 / 8550 の始値を回収** → 主サンプル3件の真のギャップを確定させる
   （`BuildDailyStats` は回さない。手打ち数式で）
3. 紙トレード継続。**寄り付き型20件が目標**
4. **毎日引け後にスナップショットを取る**（退避 → (1)取得 → (2)数式固定 → 保存 → コミット）
5. 次回シグナル日に **B13=15:31 の効果を検証**
   （T2が15:31台／**かつ Q列の逆算価格＝MS2終値**）
6. 20件到達後、M/N列と O/P列から最適TP/SLを逆算。
   **その際は「真の寄り付きギャップ」でも層別して比較する**

---

## やらないことリスト

| 案 | 判断 | 理由 |
|---|---|---|
| Python + J-Quants へ移行 | 見送り | RssChartPast で日足が取れる |
| 全3,117銘柄の再スクリーニング | 見送り | 紙トレ検証が先 |
| イベント銘柄の自動抽出 | 凍結 | RSSの決算発表日は過去日付しか返さない |
| B2（損切り率）を緩める | **却下** | 1件の結果論でリスク管理を動かすのは最悪の意思決定 |
| **シグナル条件を始値ベースに変更する** | **却下（今は）** | 20件ルール。**頻度は落ちないことが分かったが（0.55〜0.78件/日）、変えると比較対象が消える。** 記録は増やすが判定は変えない |
| 「1日1回だけ発火」ルールを外す | **却下** | 8/20 の松屋は寄り付き+1.32%でそもそも対象外だった |
| エントリーを 9:03〜9:05 の押し目まで待つ | **保留** | 別戦略になる。20件で寄り天率を見てから |
| B3を+4%に戻す | 却下 | n=5で判断するのは早計 |
| Watchlistを増やす | 保留 | コア30の選定基準を緩めるとサンプルの均質性が崩れる |
| **コア30を「ギャップ頻度」で選び直す** | **保留** | 8237・8628 など20日で0日の銘柄がいる。ただし選定20日と評価20日が同じで選択バイアスがあるため、別期間のデータを取ってから |
| **`BuildDailyStats` を再実行する** | **却下（当面）** | `日足集計` と `選別候補` を `Cells.Clear` する。ベースレートの一次データが消える |
| 8/10 の決済時刻・保有時間を推定値で補完 | 却下 | 推定値混入のリスクのほうが大きい |
| 8/19 の O5/P5 を補完 | 却下 | 経路情報は終値から復元できない |
| **8/14 Q4 / 8/20 Q6 を「終値」として集計に使う** | **却下** | 打ち切り時点の値。真の終値はスナップショット側から別途算出する |
| 無人日の監視起動 | 却下 | エントリー記録ができずデータにならない |
| 境界時刻を9:05に緩める | 却下 | 唯一の根拠が「件数が集まりやすい」（8/18決着） |
| 取引日の朝にコードを触る | 却下 | 8/12 の再演リスク。変更は必ず引け後 |
| **同名モジュールをそのまま Import する** | **却下** | `ThisWorkbook1` / `modConfig1` を生む。**標準モジュールでも先に「解放」してから Import** |
| ドキュメントモジュールを Import File で入れる | 却下 | コードペイン直接貼り付け一択 |
| **`src/*.bas` をシステムのバックアップとみなす** | **却下** | `frmController` のレイアウト（.frx）・RSS数式・Config・日足集計は復元できない。**xlsm が唯一の完全バックアップ** |
| `TestCheckGapSignalsNow` の実行 | 非推奨 | Logに偽シグナルが混入する |
| CommandBars 方式のUI | 却下 | Top/Leftが(-9,-9)固定。UserForm で解決済み |
| Position の古い行を手で消す | 却下 | 次のエントリーで自動上書きされる |
| STATE.md の「実装済み」記述を信じる | **却下** | 8/19 に `[L]`、8/20 に `[M]` と `[J]` で刺された。**xlsm実物を監査してから判断する** |
| 1日に2件のコード変更をまとめて入れる | **却下** | 不具合が出たとき切り分け不能 |
| Watchlistに始値列を追加する | **不要** | スナップショットが既に始値を取っている。場中のRSS負荷を増やさずに済む |
| スナップショット取得前に退避を省く | **却下** | `ws.Cells.Clear` で前日分が消える |

---

## 既知の未修正issue

| # | 内容 | 影響 |
|---|---|---|
| 1 | `CalcPositionSize` が `Int(rawShares/100)*100` | 株価5,000円超で0株を返す。コア30の最高値は3,080円（6023）なので現状は表面化しない |
| 2 | 同上の切り捨て | 8/19 の 9278（2,858円）で実リスクが1%→**0.57%** に低下。株価が高い銘柄ほどリスクが目減りする |
| 3 | 監視窓が10:00まで | ザラ場型を拾う。設計通りだが二重集計ルールで対処 |
| 4 | ~~Position E列が「数式固定」で潰れる~~ | **削除。事実誤認（8/19確認）** |
| 5 | ~~`modConfig` の `Find` が `LookAt:=xlPart`~~ | **解消（8/20）。完全一致優先に変更** |
| 6 | Trades V列のヘッダ名がコードと不一致 | コードは「発火区分」、シート実物は「パターン区分」。既存値があるため実害なし |
| 7 | ランナー保持に上限時間がない | 含み益1.5%超が続く限り強制手仕舞い（15:00）まで保持 |
| 8 | **追跡チェーンの保護がメモリ依存** | VBA状態リセットで Watchdog ごと消える。`[L]` の起動時警告で事後検知するのみ |
| 9 | **`[H]` はシグナル後にメモリが死んだ日を記録できない** | `StopGapMonitoring` 経由でしか書かれない。8/19がその状態 |
| 10 | **`ギャップ率` が始値ベースでない** | 寄り付きギャップと時点騰落率を区別できない。**検証できた2件（8/19・8/20）が2件とも真のギャップではなかった。** 夜間スナップショットで始値を日次記録し、20件時に層別して評価する |
| 11 | ~~設定値が効いているか確認する手段がない~~ | **解消（8/20）。`ShowLoadedConfig` 追加** |
| 12 | **`FinalizeTracking` が価格を読まない** | Q列・O/P列に入るのは常に `TrackEndTime` 直前の読み取り値。**8/14・8/20 が該当。** B13=15:31 で実害は消える見込みだが、コード自体は未修正（打ち切り時に1回読んでから確定する形が本来） |
| 13 | **ベースレートに選択バイアスがある** | `日足集計` の20日は、コア30を選ぶのに使った20日と同一。前向きの期待値としては上振れしている。別期間で取り直すまで参考値扱い |

---

## 決済理由の表記について

Trades の「時間切れ」は実態と合っていない。実際の判定は：

```vba
If elapsedMin >= TimeStopMinutes And plPct < TakeProfitPct / 2 Then
```

15分経過「かつ」含み益が利確の半分未満、の **AND 条件**。実質は**「ランナー失格」**。
B3（利確率）はこの失格ラインも兼ねているため、変更すると2つの意味が同時に動く**二重レバー**。

---

## 空のままで正常なシート

| シート | 役割 | 備考 |
|---|---|---|
| 全銘柄候補 | 月次スクリーニングの一時作業場 | **空が正常** |
| イベント銘柄一覧 | 決算発表日ベースの抽出先 | RSS制約により構造的に永久に空 |
| _work | `modDaily7` の並列取得作業場 | **空が正常** |

---

## UI構成

| モジュール/フォーム | 役割 |
|---|---|
| `frmController`（UserForm） | 操作パネル本体。CommandButton×8個 |
| `modUIButtons.ShowController` | パネルをモードレス表示（多重表示防止つき） |
| `ThisWorkbook.Workbook_Open` | `ShowController` ＋ `[L]` 追跡未完走の警告 |
| `modConfig.ShowLoadedConfig` | **Alt+F8 から実行。読み込まれた設定値を一覧表示** |

ボタン構成：監視開始／監視停止／エントリー記録／決済記録／監視停止(緊急)／
(1)スナップショット取得／(2)数式固定／閉じる

### VBAProject の構成（8/20時点・実物確認済み）

- 標準モジュール（**9本**）: `modConfig` `modDaily7` `modMonitor` `modNotify`
  `modPosition` `modScreening` `modSnapshot` `modUIButtons` `modWatchdog`
- フォーム: `frmController`
- ドキュメントモジュール: `ThisWorkbook` ＋ `Sheet1`〜`SheetN`（コードなし）

> **異常検知の対象は「標準モジュール9本」だけにすること。**
> `SS_MMDD` を毎日作る運用なので `SheetN` は単調に増える（8/20時点で Sheet14）。
> ここを異常判定に使うと毎日誤発報する。

---

## 注意事項

- `modDaily7` の `BASE_DATE` は再実行時に必ず更新。**ただし再実行は `日足集計` を消す**
- `modScreening` の JPXファイルパスは **`Environ$("USERPROFILE")` ＋ 相対パス定数**から組み立てる。
  ユーザー名はコードにもドキュメントにも埋め込まない。JPXファイルは月次更新（毎月第3営業日）
- Watchlist が30件を超えると RSS 500銘柄上限に近づく（現在120登録）
- 作業を中断して閉じるときは、スナップショットの数式を値に固定してから保存
- `.bas` ファイルは必ず Shift-JIS(CP932)／CRLF で保存。UTF-8だとVBEで文字化けする
- **同名モジュールの差し替えは「解放（削除）→ Import」。** そのまま Import すると
  `modConfig1` のような別モジュールが増えて、元のコードが生き続ける
- **ドキュメントモジュール（ThisWorkbook / Sheet系）は Import File で置き換えられない。**
  コードペインを全選択→削除→貼り付け。`Attribute VB_...` 行は貼らない
  （`src/ThisWorkbook.cls` は Attribute行を除いた状態で置いてある）
- **Config を変更したら `ShowLoadedConfig` で必ず目視確認する。**
- **Public/モジュール変数はVBEの停止ボタン・コード編集・`End`でリセットされる。
  リセットすると Watchdog と追跡チェーンが同時に死ぬ。場中はVBEを開かない**
- **`On Error Resume Next` を書くときは、握りつぶした先が安全側かを必ず確認する。**
  `[M]` はこれが原因で長期間不可視だった
- **「打ち切り条件を先に判定して抜ける」ループは、最後の1回を読まない。**
  `[J]` / issue#12 はこれが原因。`TrackAfterExit` と同じ形のコードを書くときは必ず疑う

---

## GitHub 運用ルール

| いつ | 何を |
|---|---|
| 取引日の終了後 | `STATE.md`、`data/trades.csv`、**`デイトレ用.xlsm`** |
| VBAを変更した直後 | `src/*.bas` / `src/ThisWorkbook.cls`（変更したモジュールのみでよい） |
| 新しい実測が取れたとき | `KNOWLEDGE.md`（追記のみ） |

> **xlsm は「週1回」では緩い。** Trades と Config は毎日変わり、
> `frmController` のレイアウトと RSS数式を持つのはこのファイルだけ。
> 324KB／回なので、毎取引日コミットしても20件到達（10月中旬）まで累計3MB程度にしかならない。

### 今回のコミットメッセージ

```
fix(state): Q6は終値ではなかった／始値ベースの検証結果とベースレートを追記

Trades Q6=1.17%(1,471円)は当日終値1,470円ではない。
modPosition.TrackAfterExit は打ち切り判定を価格読み取りより先に行い、
FinalizeTracking は値を読まないため、Q列には常に TrackEndTime 直前の
読み取り値が入る。8/14 Q4 と同一構造（issue#12）。
決済時刻の秒と T列の打ち切り時刻が15秒周期で一致することで裏取り済み。
B13=15:31 によりオークション後の読み取りが入るため実害は消える見込み。

- 8/20 も真の寄り付きギャップ3%以上は 0件（最大 9505 +2.32%）
- 8/19 のランキングは SS_0819 から再計算して全数一致を確認
- modDaily7 の gap は真の寄り付きギャップ（Abs(始値-前日終値)/前日終値）。
  日足集計にコア30の600銘柄日ぶんのベースレートが既に存在していた。
  |gap|>=3% は 5.17%。ただし選定に使った20日と同一で選択バイアスあり(issue#13)
- 始値ベースに変えても頻度は落ちない（上方向 0.55〜0.78件/日）ことを確認。
  「判定は変えない」根拠は「比較対象が消えるから」の一本に整理
- 8/10・8/13・8/14 の始値は RssChartPast で遡及取得可能（MS2手読み不要）
- 異常検知の対象を「標準モジュール9本」に限定（SS_MMDD で SheetN が増えるため）
- JPXパスは Environ$("USERPROFILE") 方式である旨に修正
- GitHub運用ルール：xlsm を週1回→取引日ごとに変更
```
