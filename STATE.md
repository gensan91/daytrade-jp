# STATE — デイトレ自動化プロジェクト 現在地

最終更新：2026/8/17（月）／このファイルは**毎回上書き**。技術知見は `KNOWLEDGE.md` へ。

---

## まず最初にやること

```
1. マーケットスピードII を起動してログイン
2. デイトレ用.xlsm を開く（マクロ有効化 → 自動でデイトレ操作パネルが表示される）
3. Watchlist C:F に RSS数式が入っているか確認（=RssMarket($A2,"項目名")）
4. Position E2 に =RssMarket(A2,"現在値") が入っているか確認 ← 忘れると損益判定が全て素通り
5. 8:55 に 操作パネルの「監視開始」ボタンを押す
```

**次回の取引日は未定**（8/17 は通院のため監視なし。診察結果を踏まえて決める）。

**操作パネルが出ていない場合：** Alt+F8 → `ShowController` を実行。

---

## 8/17 に完了した作業（相場に触れずに実施）

| # | 内容 | 状態 |
|---|---|---|
| 1 | Config B13 を `15:30` → `15:31` に変更 | ✅ |
| 2 | Trades I2・Q2・Q3 を削除（データ純度違反の解消） | ✅ |
| 3 | Trades D列・I列の型を datetime に統一 | ✅ |
| 4 | `modMonitor.bas` 差し替え（`[G]` Watchdog 誤検知の3秒窓を封鎖） | ✅ |

**8/17 は監視を実施していない。** 無人稼働はデータが取れないうえ、ポップアップ滞留でタイマー連鎖が止まり 8/12 と同種の判定不能な記録が残るため、意図的に起動しなかった。

---

## 現在の状態

| 項目 | 状態 |
|---|---|
| コア30銘柄 | ✅ 確定 |
| Config B1（ギャップ率閾値） | ✅ **3%** |
| Config B3（利確率） | ✅ **3%** |
| Config B13（終値追跡終了時刻） | ✅ **15:31**（8/17変更。クロージング・オークション対応） |
| Config B14（往復コスト率） | ✅ 0.4 |
| Watchlist RSS数式 | ✅ 投入済み（C:F） |
| Trades シート（恒久履歴・22列） | ✅ 稼働中。V列「パターン区分」まで整備 |
| 大引け追跡（O/P/Q列） | ✅ 8/14に初完走。ただし Q列は 15:31 変更前のため「引け直前値」 |
| modWatchdog（60秒の見張り役） | ✅ 導入済み。`[G]` 適用済み |
| 紙トレード | 🟡 **有効3件 / 20件**（寄り付き型2／ザラ場型1） |
| イベント銘柄 | ❌ 未実装（RSS「決算発表日」が構造的に使えないため凍結） |

---

## トレード記録（Trades シート＝唯一の正）

| 日付 | 銘柄 | 検知 | エントリー | 決済 | 理由 | 損益率 | コスト後 | 区分 |
|---|---|---|---|---|---|---|---|---|
| 8/10 | 4813 ＡＣＣＥＳＳ | 9:00:16 | 393 | 404.8 | 利確 | +3.00% | +12,274円 | 寄り付き型 |
| 8/13 | 7322 三十三FG | 9:00:44 | 1975 | 1992 | ランナー失格 | +0.86% | +1,820円 | 寄り付き型 |
| 8/14 | 8550 栃木銀行 | 9:08:17 | 1115 | 1132 | ランナー失格 | +1.52% | +5,016円 | ザラ場型 |

**3勝0敗／コスト後累計 +19,110円。**
8/12 は監視の稼働状態に疑義があり、検証件数にカウントしない（Log に記録済み）。

### データ純度に関する取り決め

- **8/10**：決済価格は +3.00% からの逆算値。決済時刻（I2）・保有時間（R2）は推定になるため**空欄のまま**とする。終値ベース損益率（Q2）も由来不明のため削除済み。U2 の注記が根拠
- **8/13**：旧コード期のため大引け追跡（O/P列）が存在しない。取得不能であり、後から埋めない。Q3 も同様に削除済み
- **8/13 は基準の混在したサンプル。** 価格は気配値1975、時刻は現在時刻9:07:35 を採用したため、記録開始時点で既に +0.86% だった（N3 の保有中MAE が +0.86 なのはこのため）。**+0.86% は上振れバイアスを含む**。20件集計時は外れ値として扱うか、感度分析で除外して確認する
- **8/14 の Q4（-0.45%）は「終値」ではなく「引け直前値（15:29:47頃）」。** B13 が 15:30 だったため、15:30 のクロージング・オークションを取り逃している。8/17 に B13 を 15:31 へ変更済みなので、次回以降は真の終値を捕捉する

---

## 【重要】サンプルの二重集計ルール（8/14 制定）

シグナルには性質の異なる2種類が混在している。**混ぜて統計を取ると意味のない数字になる**ため、V列「パターン区分」で分離して集計する。

| 区分 | 定義 | 位置づけ | 現在の件数 |
|---|---|---|---|
| 寄り付き型 | 板寄せの需給不均衡が源泉 | **主サンプル。目標20件** | 2件 |
| ザラ場型 | 継続中のトレンドが源泉 | 並行記録。別集計 | 1件 |

- 最適TP/SLの逆算は**寄り付き型20件が揃ってから**、寄り付き型のみで行う
- ザラ場型は件数が貯まった時点で別途評価する。件数目標は設けない
- **どちらの区分でも、20件到達までパラメータは一切変更しない**

### ⚠️ 未解決：区分の境界時刻

**このドキュメントとコードで定義が食い違っている。**

| | 定義 |
|---|---|
| 旧 STATE.md の記述 | 9:00〜9:05 |
| `modPosition.ArchiveTrade` の `[I]` | `<= 9:01:00` |

現状は **9:01 が実際の挙動**。9:03 のシグナルは黙ってザラ場型に分類される。
どちらに揃えるかは**判断待ち**。件数が増える前に決着させること。

- **9:01 案**：「板寄せの需給不均衡」という定義に忠実。寄り付き直後の数分は既に連続取引であり、源泉が異なる
- **9:05 案**：サンプルが集まりやすい。20件到達までの日数が短縮される

---

## 次にやること（優先順）

1. **寄り付き型の境界時刻を 9:01 / 9:05 のどちらかに決定**し、ドキュメントとコードを一致させる
2. 紙トレード継続。**寄り付き型20件**が目標
3. 次の監視日、Log に「復旧」行が出ないことを確認（`[G]` 適用後の初回検証）
4. 20件到達後、M/N列（保有中MFE/MAE）と O/P列（大引けMFE/MAE）から最適TP/SLを逆算
5. **この間はスクリーナーを触らない**

---

## やらないことリスト

| 案 | 判断 | 理由 |
|---|---|---|
| Python + J-Quants へ移行 | 見送り | RssChartPast で日足が取れる |
| 全3,117銘柄の再スクリーニング | 見送り | 紙トレ検証が先。8月下旬に再実行 |
| イベント銘柄の自動抽出 | 凍結 | RSSの決算発表日は過去日付しか返さない。J-Quants / TDnet / 手動入力の3択だが、20件完了まで着手しない |
| スコアの重み調整 | 保留 | 実データ待ち |
| B3を+4%に戻す | 却下 | n=3で判断するのは早計 |
| 8/10 の決済時刻・保有時間を推定値で補完 | 却下 | 時間切れ15分ルールがある以上、保有時間の分布は既知。推定値混入のリスクのほうが大きい |
| 無人日の監視起動 | 却下 | エントリー記録ができずデータにならない。ポップアップ滞留でタイマー連鎖が止まり、記録自体が信用できなくなる |
| 8:59 誤検知の観察による `[F]` 検証 | 中止 | 観察結果に関わらず打ち手（`[G]` の修正）が同じ。意思決定価値がない |
| `TestCheckGapSignalsNow` の実行 | 非推奨 | Logに偽シグナルが混入する |
| CommandBars 方式のUI | 却下 | Top/Leftが(-9,-9)固定。UserForm（frmController）で解決済み |

---

## 既知の未修正issue

| # | 内容 | 影響 |
|---|---|---|
| 1 | `CalcPositionSize` が `Int(rawShares/100)*100` | 株価5,000円超で**0株**を返す。コア30の最高値は2,981円なので現状は表面化しない。銘柄入替時に注意 |
| 2 | 同上の切り捨て | 株価2,981円だと実リスクが1%→0.6%に低下 |
| 3 | 監視窓が10:00まで | 寄り付き型以外（ザラ場型）を拾う。設計通りだが、二重集計ルールで対処 |
| 4 | Position E列のRSS数式は手動設定 | 毎朝の確認が必要。コード側でのフォールバックなし |
| 5 | `modConfig` の `Find` が `LookAt:=xlPart` | Config A列に部分一致するラベルを追加すると誤ヒットする。ラベルは重複しない文字列で維持すること |
| 6 | Trades V列のヘッダ名がコードと不一致 | コードは「発火区分」、シート実物は「パターン区分」。既存値があるため上書きされず実害なし。V1 を消すと名前が変わる |
| 7 | ランナー保持に上限時間がない | 含み益1.5%超が続く限り強制手仕舞い（15:00）まで保持し続ける。8/13・8/14 はいずれも早期に1.5%を割ったため表面化していない |

---

## 決済理由の表記について

Trades の「時間切れ」は実態と合っていない。実際の判定は：

```vba
If elapsedMin >= TimeStopMinutes And plPct < TakeProfitPct / 2 Then
```

**15分経過「かつ」含み益が利確の半分未満**、の AND 条件。15分時点で1.5%超なら「ランナー」として保持を継続する。8/13 の19.3分、8/14 の17分はこの仕様通りの挙動であり、バグではない。

実質は**「ランナー失格」**。B3（利確率）はこの失格ラインも兼ねているため、**変更すると2つの意味が同時に動く二重レバー**である点に注意。

---

## 空のままで正常なシート

| シート | 役割 | 備考 |
|---|---|---|
| 全銘柄候補 | 月次スクリーニングの一時作業場 | **空が正常。**RSS数式が残ると500銘柄上限を食い潰し、Watchlistが取れなくなる。`GetOrCreateSheet` で毎回自動再生成されるため削除も可 |
| イベント銘柄一覧 | 決算発表日ベースの抽出先 | RSS制約により構造的に永久に空。ヘッダー5列はコードが作った跡 |

---

## UI構成

| モジュール/フォーム | 役割 |
|---|---|
| `frmController`（UserForm） | 操作パネル本体。CommandButton×8個 |
| `modUIButtons.ShowController` | パネルをモードレス表示（多重表示防止つき） |
| `ThisWorkbook.Workbook_Open` | ファイルを開いたときに自動で`ShowController`を呼ぶ |

ボタン構成：監視開始／監視停止／エントリー記録／決済記録／監視停止(緊急)／(1)スナップショット取得／(2)数式固定／閉じる

---

## 注意事項

- `modDaily7` の `BASE_DATE` は再実行時に必ず更新
- `modScreening.JPX_FILE_PATH` はハードコード。JPXファイルは月次更新（毎月第3営業日）
- Watchlist が30件を超えると RSS 500銘柄上限に当たる
- 作業を中断して閉じるときは、**数式を値に固定してから保存**
- `.bas` ファイルは必ず **Shift-JIS(CP932)** で保存。UTF-8だとVBEで文字化けする
- `LoadConfig` は無反応が正常（Public変数への代入のみ）。確認はイミディエイトウィンドウで `?Format(TrackEndTime,"hh:mm"), RoundTripCostPct`
- Public変数はVBEの停止ボタン・コード編集・`End`でリセットされる

---

## GitHub 運用ルール

### コミットするタイミング

| いつ | 何を |
|---|---|
| 取引日の終了後 | `STATE.md`、`data/trades.csv` |
| VBAを変更した直後 | `src/*.bas`（変更したモジュールのみでよい） |
| 週1回 | `デイトレ用.xlsm`（バイナリ・バックアップ用途） |

### `.bas` のエクスポート手順

VBE で対象モジュールを右クリック →「ファイルのエクスポート」→ `src/` に上書き保存。
**変更履歴コメント（`[A]`〜`[G]`）は今後増やさない。** Git の差分が履歴の役割を果たすため、コメントは「なぜそうしたか」だけを残し、「いつ変えたか」は書かない。

### コミットメッセージの書き方

```
fix(monitor): Watchdog誤検知の3秒窓を封鎖
data(trades): 8/17 型統一・純度違反セルの削除
docs(state): 8/17 現在地を更新
```

先頭のタグだけ揃えておけば、後から `git log --oneline` で追える。
